# OpportunityHub — COMPLETE Supabase schema
# Paste this entire file into: Supabase Dashboard → SQL Editor → New query → Run
# Then run: supabase/seed-opportunities.sql (optional sample data)

See `complete-schema.sql` in this folder — it is the single source of truth.

Quick fix for "Could not find the table 'public.profiles'":
1. Open Supabase SQL Editor
2. Paste and run `complete-schema.sql`
3. Wait a few seconds (or re-run the NOTIFY at the bottom)
4. Try signup again
